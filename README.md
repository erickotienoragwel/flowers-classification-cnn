# 🌸 Flower Classification using CNN

A deep learning project that classifies images of flowers into multiple categories using a Convolutional Neural Network (CNN).

## 🧠 Overview
This project builds and trains a CNN model to identify flower species from images. It uses TensorFlow/Keras for model development and training.

## 📁 Project Structure



## ⚙️ Requirements
- Python 3.x  
- TensorFlow / Keras  
- NumPy  
- Matplotlib  
- scikit-learn  

Install dependencies:
```bash
pip install -r requirements.txt

